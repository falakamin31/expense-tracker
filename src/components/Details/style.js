import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  income: {
    borderBottom: '10px solid rgba(7, 105, 63)',
  },
  expense: {
    borderBottom: '10px solid rgba(255, 0, 0)',
  },
}));